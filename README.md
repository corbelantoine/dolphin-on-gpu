# dolphin-on-gpu

### Depedencies

cgal : quadratic problem solver

**Unix**: `sudo apt-get install libcgal-dev`

**Mac**: `brew install cgal`

**Windows**: Really ??

### Running project:

**Compile**: `make`

**Run**: `./dolphin`
